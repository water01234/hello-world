

# 数学

## 数分

### 集合

#### 集合的==定义==

**定义：** 具有某种特定性质的具体的或抽象的对象汇集成的总体

元素

##### 元素的表示

常用集合表示

#### 集合的==表示==

1. 枚举法：例 A={a，1}, N^+^={1,2,3,...,n,...}
1. 描述法：例 S={x|x具有性质P}

#### 元素和集合的==关系和表示==

**关系：**属于，不属于

集合和集合的==关系和表示==:

**关系**：包含于,不包含
$$
{S}\subset {T} :\forall{s}\in{S},{s}\in{T}
$$

$$
{S}\not\subset{T} :\exists{s_{0}}\in{S},{s}\notin{T}
$$

==真子集==
$$
{S}\subsetneq{T}:\left({S}\subset{T}\right)\land\left({S}\not\supset{T}\right)
$$

==集合相等==
$$
{S}={T}:\left({S}\subset{T}\right)\land \left({{S}\supset{T}}\right)
$$
==特殊==
$$
\varnothing\subset{S}\subset{S}
$$
==区间==：实数范围

#### 集合的==一般性质==

1. 无序性：{a,b}j={b,a}
2. 互异性：{a,a,b}={a,b}
3. ==空集==：不包含任何元素的集合

#### 集合的==运算==

==并(二元关系：1，2)==
$$
S\cup T = \left\{x|x\in S,或者x\in T\right\}
$$
==交（2）==
$$
S\cap T = \left\{x|x\in S,并且x\in T\right\}
$$
==差（1）==
$$
S\setminus T\left\{x|x\in S,并且x\notin T\right\}=S\cap T_{X}^{C}
$$
==补==
$$
S_{X}^{C}=X \setminus S
$$
==空集(0)==

==对称差(2)==
$$
S△T=\left(S \setminus T\right)\cup\left(T\setminus S \right)
$$


==韦恩图==

==性质==

并，交：交换律，结合律，分配律

补:
$$
S\cup \left( S^c\right)=U
$$

$$
S\cap \left( S^c\right)=\varnothing
$$



对偶律：
$$
\left(A\cup B\right)^c= \left( A^c\right)\cap
\left(B^c\right)
$$

$$
\left(A\cap B\right)^c= \left( A^c\right)\cup
\left( B^c\right)
$$
两次反：
$$
\left( \left(T\right)^c\right)^c= T
$$

包含与运算的互换
$$
{S}\subset {T}\Longleftrightarrow \left(S\cap T=S\right)\Longleftrightarrow \left(S\cup T= T\right)
$$
不等式：
$$
\left(S\cap T\right)\subset{S}\subset \left(S\cup T\right)
$$

交换，分配，结合，消去

单位元，零元

逆元，负元

#### 无限集和有限集

==有限集定义==：若集合S由n个元素组成，n是确定的**非负整数**，则称集合S为有限集

==无限集定义==：不是有限集的集合称为无限集

==可列集定义==：如果一个**无限集**中的元素可以按**某种规律排成一个序列**，或者说，这个集合可表示为
$$
\left\{a_1,a_2,a_3,...,a_n,...\right\}
$$
**定理1.1.1**:可列个可列集之并也是可列集(用了**对角线法则**)

#### 笛卡尔集

定义：
$$
A\times B=\left\{\left(x,y\right)|x\in A并且y\in B\right\}
$$
特别地，当A与B都是实数集$$\mathbb{R}$$时，$$\mathbb{R}$$×$$\mathbb{R}$$(记作$$\mathbb{R^2}$$​)表示的是平面Descartes直角坐标系下用坐标表示的点的集合。

### 映射和函数

#### 映射

映射是指两个集合之间的一种对应关系
==映射定义==：设X,Y是两个给定的集合，若按照某种规则f,使得对集合X中的每一个元素x,都可以找到集合Y中==惟一确定==的元素y与之对应，则称这个对应规则f是集合X到集合Y的一个映射
$$
f: X \rightarrow Y, \quad x \mapsto f(x)
$$

**值域**：$R_f=\left\{y|y\in Y,y=f(x)并且x\in X\right\}$​

**单射**：设f是集合X到集合Y的一个==映射==，若f的逆像也具有惟一性，即==若$y_1=y_2$,则$x_1=x_2$==

**满射**：$R_f=Y$

**双射**：单射且满射

**逆映射**：设$f:X\rightarrow Y$ 是==单射==
$$
f^{-1}: R_f \rightarrow X, \quad y \mapsto x(其中f(x)=y)
$$

只要逆映射$f^-1$==存在==，$f^-1$​就一定是==双射==

**复合映射**：前提${R_g}\subset{D_f}$
$$
f\circ f^{-1}(y)=y,y\in R_f
$$

$$
f^{-1}\circ f(x)=x ,x\in D_f
$$



## 高代
