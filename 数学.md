

# 数学

## 数分

### 集合和映射

#### 集合的==定义==

**定义：** 具有某种特定性质的具体的或抽象的对象汇集成的总体

**元素**

##### 元素的表示

**常用集合表示**

#### 集合的==表示==

1. 枚举法：例 A={a，1}, N^+^={1,2,3,...,n,...}
1. 描述法：例 S={x|x具有性质P}

#### 元素和集合的==关系和表示==

**关系：**属于，不属于
$$
{S}\subset {T} :\forall{s}\in{S},{s}\in{T}
$$

$$
{S}\not\subset{T} :\exists{s_{0}}\in{S},{s}\notin{T}
$$

==真子集==
$$
{S}\subsetneq{T}:{S}\subset{T}\land {S}\not\supset{T}
$$

==集合相等==
$$
{S}={T}:{S}\subset{T}\land {{S}\supset{T}}
$$
==特殊==
$$
\varnothing\subset{S}\subset{S}
$$
==区间==：实数范围

#### 集合的==一般性质==

1. 无序性：{a,b}j={b,a}
2. 互异性：{a,a,b}={a,b}
3. ==空集==：不包含任何元素的集合

#### 集合的==运算==

==并(二元关系：1，2)==
$$
S\cup T = \left\{x|x\in S,或者x\in T\right\}
$$
==交（2）==
$$
S\cap T = \left\{x|x\in S,并且x\in T\right\}
$$
==差（1）==
$$
S\setminus T\left\{x|x\in S,并且x\notin T\right\}=S\cap T_{X}^{C}
$$
==补==
$$
S_{X}^{C}=X \setminus S
$$
==空集(0)==

==韦恩图==

==性质==

并，交：交换律，结合律，分配律

差:
$$
S\cup \left(T\setminus S\right)=T
$$

对偶律：
$$
S\setminus \left(A\cup B\right)= \left(S\setminus A\right)\cap
\left(S\setminus B\right)
$$

$$
S\setminus \left(A\cap B\right)= \left(S\setminus A\right)\cup
\left(S\setminus B\right)
$$
两次反：
$$
S\setminus \left(S\setminus T\right)= T
$$


## 高代
