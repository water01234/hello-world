# 数学

## 数分

### 集合和映射

#### 集合的==定义==

**定义：** 具有某种特定性质的具体的或抽象的对象汇集成的总体

**元素**

##### 元素的表示

**常用集合表示**

#### 集合的==表示==

1. 枚举法：例 A={a，1}, N^+^={1,2,3,...,n,...}
1. 描述法：例 S={x|x具有性质P}

#### 元素和集合的==关系和表示==

**关系：**属于，不属于
$$
{S}\subset {T} :\forall{s}\in{S},{s}\in{T}
$$

$$
{S}\not\subset{T} :\exists{s_{0}}\in{S},{s}\notin{T}
$$

==真子集==
$$
{S}\subsetneq{T}:{S}\subset{T}\land {S}\not\supset{T}
$$

==集合相等==
$$
{S}={T}:{S}\subset{T}\land {{S}\supset{T}}
$$
==特殊==
$$
\varnothing\subset{S}\subset{S}
$$
==区间==：实数范围

#### 集合的==一般性质==

1. 无序性：{a,b}j={b,a}
2. 互异性：{a,a,b}={a,b}
3. ==空集==：不包含任何元素的集合

#### 集合的==运算==



## 高代
